# Bologna Now

A smart bilingual (IT/EN) Bologna city guide app built with React + Vite.

## Setup

```bash
npm install -g pnpm
pnpm install
pnpm --filter @workspace/bolo-viva run dev
```

## Environment variables

Copy `.env.example` to `.env` and fill in your keys.
